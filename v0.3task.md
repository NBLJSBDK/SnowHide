# SnowHide v0.3task.md —— 架构基础工程施工任务

> 状态：**v0.3.0、v0.3.1、v0.3.2 已完成 Release 真机回归并发布；当前主线进入 `0.4.0-dev`，基线 Tag 为 `v0.4`；Android instrumentation 待补**
>
> 本文不是产品功能清单，而是 SnowHide v0.3.x 的架构施工图、边界合同和验收记录。
> v0.3 的主要产物不是“再加几个按钮”，而是把已经能用的 v0.2 代码逐步收敛成可维护、可测试、可扩展的工程。

---

## 0. 基线和执行规则

### 0.1 Git 与版本基线

| 项目 | 值 |
|---|---|
| 架构基线 commit | `f68093d` |
| 最后一个 v0.2.x tag | `v0.2.2` |
| v0.2.2 内容 | Recent 提示修正 + 架构指导文档 |
| v0.3 首个目标版本 | `0.3.0` |
| `versionCode` | 永远为 `1` |
| 当前 `versionName` | `0.3.2`，v0.3.2 已完成更新说明 |
| 最近正式 Tag | `v0.3.1`（提交 `8349cbc`） |
| v0.3.2 内容 | 修复主屏宫格文件夹顺序与循环滑动页面顺序不一致，新增回归测试 |

`v0.2.2` 是不可移动的历史基线。本文创建和审阅期间不修改版本号；开始第一批 v0.3 代码前，才把 `app/build.gradle.kts` 的 `versionName` 改为 `0.3.0`。

### 0.2 文档和提交纪律

- 本文件先创建、先审阅；代码施工阶段允许按可编译边界提交，但未经用户明确授权不 push、tag。
- 第一个 v0.3 APK 必须完成真机验收并获得用户确认后，才可创建最终发布 tag。
- 每个可编译的小阶段单独提交，不把多个不相关模块揉成一个大提交。
- 第一个 v0.3 APK 已完成用户确认并创建 `v0.3.0` tag；后续版本继续遵循同样的验收流程。
- 任何旧 tag 不移动、不删除、不复用。
- 不使用 `git amend`、强制推送或危险回滚命令。
- 编译后可以执行 `adb install -r` 验证，但不卸载、不清数据、不修改设备上的包管理器状态，除非用户明确授权。

### 0.3 任务状态标记

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成并通过验收
- `[!]` 有风险、阻塞或需要用户决定
- 每个任务完成后必须补充：实际文件、行为不变量、验证命令、真机结果、commit。

---

## 1. v0.3 的核心目标

### 1.1 总目标

在不破坏 v0.2 已验证行为的前提下，建立以下基础：

1. Android 系统入口统一通过手写组合根获得依赖。
2. `feature → domain → data → core` 的新代码依赖方向真正可执行。
3. 数据、业务规则、系统入口、视觉组件各自拥有清晰边界。
4. 冻结执行、Shizuku Binder、备份导入和状态查询具备安全边界。
5. 关键纯逻辑能够脱离 Android 和 Compose 做单元测试。
6. 未来的多用户、休眠、多引擎、壁纸、文件夹高级设置和应用桥有明确落点，但不提前制造无调用者的空壳。
7. 以后优化 UI 主要修改 `feature` 的无状态绘制层和 `ui` 组件层，不再触碰冻结业务。

### 1.2 v0.3 的工程判断

本项目暂不拆成多个 Gradle module。

当前代码量适合先做**单 Gradle module 内的逻辑模块化**：通过 package、依赖方向、组合根、接口和测试建立边界。等边界稳定、确实出现编译隔离或构建速度收益后，再评估 Gradle 多模块。现在引入多模块只会增加装配、资源和调试复杂度。

本项目不引入 Hilt、Koin、Room、事件总线或其他框架解决边界问题。依赖装配使用手写 `CompositionRoot`，数据继续使用现有 SP+JSON 协议。

---

## 2. 当前真实状态和主要债务

这部分是施工前的事实记录，不是对已验证功能的否定。

### 2.1 已经稳定、默认不重写的部分

- Shizuku UserService Binder 方案已经经过真机验证。
- `PowerEngine`、`EngineManager`、`EngineRegistry` 的引擎抽象方向正确。
- `GridRepository` 的宫格、文件夹、重复成员清理和 LazyGrid key 防护已经验证。
- Recent 的首次基线保护、即时单包冻结、持久化补执行队列、雪藏自身排除已经验证。
- 图标包广播协议、`appfilter.xml` 解析、图标缓存和冻结应用图标回退已经验证。
- 批量执行、进度条、快捷方式保活和 Release R8 keep 规则都有真机经验。

### 2.2 P0 结构问题

1. 没有组合根。
   - `MainActivity`、`ShortcutActionActivity`、`QuickToggleTileService`、`LockCleanReceiver`、`RecentSwipeController` 各自初始化依赖。
   - TileService 冷启动时可能没有初始化引擎和 Grid。
   - 多入口同时启动时，Repository 的初始化和 StateFlow 可能竞态。

2. 特权命令边界过宽。
   - `PowerEngine.exec(String)` 目前接受任意命令字符串。
   - 业务层将包名拼进 `sh -c` 命令。
   - 备份导入可以把未经校验的字符串写入宫格数据，必须在高权限命令前拦截。

3. Binder 连接生命周期不完整。
   - 多入口可能并发 bind。
   - 绑定失败、断开、取消和超时路径需要统一状态机。
   - UserService 协议已经稳定，安全修复不得破坏 descriptor、事务码和请求格式。

4. 状态查询可能把“未知”当成“未冻结”。
   - `FrozenStateStore` 混合缓存、PackageManager 查询和 Engine 查询。
   - 查询失败时不能继续执行智能清理或批量动作。
   - 后续模式和多用户需要目标、模式、引擎身份和实际状态。

### 2.3 P1 结构问题

1. `QuickToggle` 的成员和 `opened` 状态由 ViewModel、UseCase、TileService 重复读写。
2. `BackupRepository` 直接导出整个 SP，导入不校验版本和 key 白名单。
3. `HomeScreen.kt` 约 1400 行，承担页面组合、导航、业务、副作用和视觉绘制。
4. `RecentSwipeController.kt` 约 578 行，承担无障碍、会话状态机、快照、差异、队列、执行和反馈。
5. `SettingsRepository` 依赖 UI 层的 `FreezeStyle`。
6. `QuickToggleUseCase` 直接依赖 `SharedPreferences` 和 JSON。
7. Service 直接依赖 UI 反馈工具。
8. Feature 之间直接 import，Settings 直接依赖 Recent Service。
9. `BatchProgress` 是全局共享状态，多个入口并发时可能互相覆盖。
10. 测试目录仍是模板，缺乏领域、数据、命令安全和入口冷启动测试。

---

## 3. 不可破坏的行为和协议合同

架构重构只能改变代码组织，不能悄悄改变以下行为。

### 3.1 文件安全合同

- 默认只改变包管理器状态，不删除、不修改任何目标应用数据或文件。
- 只有用户在长按菜单明确选择“移除并卸载”并二次确认时，才允许真正卸载。
- 禁止新增 `File.delete()`、`rm`、`Runtime.exec("rm")`、任意场景的 `PackageManager.deletePackage`。
- `pm` 操作必须经受控的 `PowerEngine` / UseCase 路径。
- 备份导入、恢复、Recent 队列和 UI 操作都不能绕过安全边界直接执行 shell。

### 3.2 冻结合同

- 冻结：`pm disable-user --user 0 <pkg>`。
- 解冻：`pm enable <pkg>`。
- P0 当前只有 `FREEZE` 模式。
- 冻结成功后数据保留，图标消失、进程断开、自启/通知/后台停止。
- Recent 只冻结已添加且未锁定的应用。
- Recent 候选、任务快照、队列和执行入口都排除雪藏自身。
- Recent 每次会话第一份可靠任务快照只建立基线，绝不计算差异或冻结。
- Recent 失败队列继续保留，成功或明确跳过后才移除。
- 快速清理和锁屏清理豁免锁定应用；“停用全部”仍可作用于锁定应用。

### 3.3 数据兼容合同

- SharedPreferences 主键保持：`snowhide_grid`、`snowhide_settings`。
- 现有 `grid_items`、`folders`、`folder_apps`、`locked_packages`、`frozen_cache` 字段必须兼容读取。
- QuickToggle 的 `quick_toggle_members`、`quick_toggle_opened` key 保持不变。
- Recent 校准和队列的现有 key、schema 语义保持不变。
- Backup v1 文件必须继续可读；新校验不能让合法旧备份无法导入。
- 导入失败不得把半成品写入运行时状态。
- 旧包名-only 数据未来默认解释为 user 0，不得被静默解释为其他用户。

### 3.4 核心协议合同

以下内容视为稳定协议：

- `PowerEngine` 当前方法签名和 `Result` 语义。
- 引擎 ID：`shizuku`、`root`、`do`。
- `FreezeMode.FREEZE` 的持久化字符串值。
- Shizuku UserService descriptor：`com.nbljsbdk.snowhide.IShellService`。
- 当前 Binder 事务码和请求/响应格式。
- `ShellCommandService` 的默认构造器、Context 构造器和 R8 keep 规则。
- `pm list packages -d` 和 `dumpsys activity recents` 的当前解析语义。
- `versionCode = 1`。

新增能力不得直接破坏以上合同；需要扩展时增加适配接口或能力接口，旧 API 保留兼容路径。

---

## 4. 目标架构树

以下是 v0.3.x 的目标，不代表第一天全部创建。

```text
com.nbljsbdk.snowhide/
├── MainActivity.kt                         # Activity 权限、窗口配置、AppShell
├── app/                                    # 唯一组合根和页面组合层
│   ├── SnowHideApplication.kt              # 进程启动入口，轻量初始化
│   ├── CompositionRoot.kt                  # 依赖装配和幂等初始化
│   ├── AppContainer.kt                     # 进程内依赖容器
│   ├── AppShell.kt                         # 页面组合和导航目标
│   └── AppDestination.kt                   # 导航 ID，不依赖其他 feature
├── core/                                   # 稳定抽象和纯模型
│   ├── model/
│   │   ├── PackageName.kt                  # 包名校验值对象/适配
│   │   ├── AppTarget.kt                    # 包名 + user/profile 目标
│   │   └── ControlIdentity.kt              # 引擎身份和控制来源
│   ├── engine/
│   │   ├── Engine.kt                       # PowerEngine 稳定契约
│   │   ├── EngineManager.kt                # 能力、状态和选择
│   │   ├── registry/EngineRegistry.kt      # 唯一注册表
│   │   └── impl/                           # 每个引擎独立隔离
│   ├── mode/                               # FreezeMode 和模式能力
│   ├── operation/                          # 操作 ID、进度、结果模型
│   └── feedback/                           # HapticType 和反馈协议
├── data/                                   # 持久化、缓存、数据模型
│   ├── model/
│   ├── prefs/
│   └── repo/
│       ├── GridRepository.kt
│       ├── SettingsRepository.kt
│       ├── ListOrderRepository.kt
│       ├── QuickToggleRepository.kt        # v0.3 数据所有权
│       ├── BackupRepository.kt
│       ├── RecentCalibrationRepository.kt
│       └── RecentFreezeQueueRepository.kt
├── domain/                                 # 纯业务规则和 UseCase
│   ├── control/                            # 通用应用控制门面
│   ├── backup/
│   ├── quicktoggle/
│   ├── organize/
│   ├── folder/
│   ├── recent/
│   ├── lockclean/
│   ├── shortcut/
│   └── appclone/                           # 未来多用户/分身
├── feature/                                # 页面状态和无状态 Compose
│   ├── home/
│   ├── folder/
│   ├── organize/
│   ├── appmanage/
│   ├── quicktoggle/
│   ├── settings/
│   ├── about/
│   ├── shortcut/
│   └── appclone/
├── service/                                # Android 系统入口和生命周期适配
│   ├── LockCleanAccessibilityService.kt
│   ├── LockCleanReceiver.kt
│   ├── QuickToggleTileService.kt
│   ├── RecentSwipeController.kt
│   ├── RecentTaskParser.kt
│   ├── RecentTaskSnapshotProvider.kt
│   └── ShortcutActionActivity.kt
├── platform/                               # 真实复用时才创建适配器
│   ├── command/
│   ├── feedback/
│   ├── media/
│   └── recent/
├── ui/                                     # 纯视觉、主题和 UI 工具
│   ├── components/
│   ├── theme/
│   └── util/
└── bridge/                                 # 最后才实现的外部 Intent 桥
```

### 4.1 目录创建规则

- 不能为了“看起来完整”创建空目录和无调用者接口。
- `domain/recent`、`platform/media`、`feature/appclone` 等在进入对应阶段时创建。
- 目标架构树是边界设计，不是要求一次性搬迁所有文件。
- 保持单 Gradle module；包级边界先稳定，再评估是否拆 Gradle module。

---

## 5. 依赖方向和边界规则

### 5.1 允许方向

```text
app      → feature / domain / data / core / platform / ui
feature  → domain / ui
service  → domain / core / platform
domain   → data / core
data     → core
platform → core
ui       → core
bridge   → core（需要业务时通过 domain 门面，不直接依赖 feature）
```

### 5.2 新代码严格禁止

```text
data     ✕→ ui / feature / service / domain
domain   ✕→ Compose / Activity / Service / Toast / Notification / SharedPreferences
service  ✕→ feature / ui
feature  ✕→ service / 其他 feature
ui       ✕→ data / service / feature
feature  ✕→ ShizukuEngineImpl / RootEngineImpl / DoEngineImpl
```

### 5.3 允许暂存但禁止扩大

以下是历史债务，不在第一轮全局清除；触碰相关功能时优先迁移当前调用点：

- `HomeScreen → 其他 feature`
- `SwipeDisableSettingsScreen → RecentSwipeController`
- `service → ui.util.FeedbackController/HapticController`
- `SettingsRepository → ui.util.FreezeStyle`
- `QuickToggleUseCase → SharedPreferences/JSON`
- `FrozenStateStore → EngineManager`
- `BatchExec → data.repo.BatchProgress`

新代码不得复制这些依赖，也不得为了绕过规则把业务搬到更大的全局单例中。

---

## 6. 核心设计决策

### 6.1 手写组合根，不引入 DI 框架

新增 `SnowHideApplication` 和 `CompositionRoot`。

`SnowHideApplication.onCreate()` 只执行轻量、幂等、非阻塞的进程初始化。重型应用列表扫描、图标预热、Activity 窗口设置不放进 Application。

`CompositionRoot` 负责：

- 使用 `applicationContext`。
- 统一初始化 EngineRegistry、Repository 和运行时状态仓库。
- 创建并缓存 UseCase。
- 向 Activity、Service、Receiver、Tile、Shortcut 提供同一组依赖。
- 保证多入口并发初始化不会重复或永久等待。

`AppContainer` 负责暴露依赖，不负责业务流程。第一阶段可以暂时包装现有 singleton；新代码不得直接访问 singleton，后续触碰时再逐步改为实例化实现。

### 6.2 Android 入口职责

| 入口 | v0.3 后职责 |
|---|---|
| `SnowHideApplication` | 进程级轻量初始化 |
| `MainActivity` | Shizuku Activity 权限、窗口、`AppShell` |
| `ShortcutActionActivity` | 读取受控 action、调用 UseCase、显示结果 |
| `QuickToggleTileService` | Tile 生命周期、调用 QuickToggle UseCase、更新磁贴 |
| `LockCleanReceiver` | 广播、闹钟、调用锁屏清理门面、通知 |
| `LockCleanAccessibilityService` | 无障碍生命周期、Recent 事件转发、锁屏服务注册 |
| `RecentSwipeController` | Handler、事件合并、Service 生命周期适配 |
| `AppShell` | 页面组合和导航，不承担冻结业务 |

动态锁屏广播必须有唯一注册所有者。v0.3 默认由无障碍服务负责注册，移除 MainActivity 的重复注册；如未来产品要求无障碍关闭时仍能清理，必须先设计独立、可注销、幂等的系统入口，不允许继续双重注册。

### 6.3 引擎和模式

- `PowerEngine` 现有签名不改。
- 第三方引擎 API 只出现在自己的 `impl` 文件。
- `EngineRegistry` 是唯一注册点。
- UI/domain 不 import 具体引擎实现。
- 新能力优先通过能力接口或默认失败实现扩展，不破坏现有 P0 路径。
- 未实现的休眠、隐藏、禁用、Root、Device Owner 在 v0.3 只保留设计边界，不直接开放。
- 当多个引擎真正可用时，不再仅按优先级自动切换控制身份；必须记录或明确选择执行身份。

### 6.4 目标、身份和状态

未来功能共同依赖三个概念：

1. `AppTarget`：包名 + user/profile ID。
2. `ControlIdentity`：Shizuku、Root、Device Owner 以及具体控制来源。
3. `ObservedState`：已冻结、已休眠、已隐藏、未控制、未知等实际状态。

当前 P0 的包名-only API 继续解释为 user 0。应用分身开始前，不能把现有所有 SP key 直接改成字符串拼接的 `userId:pkg`；必须先定义 schema migration、备份兼容和目标过滤规则。

### 6.5 业务层不直接产生反馈

domain 只返回结构化结果和状态，不创建 Toast、Notification、Activity、Compose 或 HapticController。

前台 feature 根据结果显示 Snackbar；后台 service 根据结果调用 platform feedback；快捷方式和磁贴使用通知兜底。

---

## 7. 数据所有权设计

### 7.1 Repository 原则

- 一个数据类别只有一个 Repository 负责读写。
- SP key 只能在对应 Repository 内出现。
- Repository 不导入 feature、service、Compose 或 UI 类型。
- Repository 对外暴露领域可用的模型和 StateFlow，不暴露 `SharedPreferences`。
- 新增字段必须考虑旧数据缺省值、备份兼容和迁移回滚。

### 7.2 QuickToggleRepository

新增：

```text
data/repo/QuickToggleRepository.kt
```

唯一负责：

- `quick_toggle_members`
- `quick_toggle_opened`
- JSON 数组编解码、去重、顺序和 StateFlow。
- 点亮时保存 opened 快照。
- 熄灭成功后清理 opened。

QuickToggleUseCase 负责成员过滤、锁定规则、执行规则和结果；ViewModel 负责搜索、排序和页面状态；TileService 只负责生命周期和磁贴状态。

### 7.3 BackupRepository 和 BackupUseCase

`BackupRepository` 保留为低层 v1 JSON/SP 读写组件。

新增 `domain/backup/BackupUseCase.kt`，负责：

- 文件大小限制。
- JSON 结构和 version 校验。
- section 和字段类型校验。
- 已知 key 白名单。
- 包名格式和当前设备可接受范围校验。
- 先完整解析，成功后再写入。
- 导入结果和需要重启的状态。

备份默认只恢复用户配置和目录结构。以下运行时数据不得被普通配置备份恢复：

- Recent 冻结队列。
- 锁屏 pending alarm 状态。
- 冻结缓存。
- 正在执行的批量进度。
- 不能跨设备可靠迁移的引擎身份和用户 ID。

旧 v1 文件必须继续可读；未知旧字段采用“忽略并报告”，不能任意写入 SP。

### 7.4 SettingsRepository

- 继续拥有设置 key 和值域校验。
- `FreezeStyle` 等持久化枚举移出 UI 层，保留原字符串值。
- 统一 clamp 延迟、透明度、布局和图标设置。
- feature/service 不再散落使用裸 SP key。

### 7.5 GridRepository

第一阶段不重写 GridRepository。

触碰相关功能时补充：

- JSON schema 校验。
- 重复 ID、孤儿 folderId、重复成员和非法包名检查。
- 旧 `GridItem.locked` 到 `locked_packages` 的一次性迁移。
- user 0 兼容默认值。

宫格的已验证排序、文件夹二级限制和整理目录即时应用行为保持不变。

### 7.6 FrozenStateStore

长期目标是拆分为：

- 运行时状态缓存。
- 引擎状态查询。
- 状态同步用例。

第一阶段不全量改写，但新代码不得把旧 Boolean 当作可靠状态。查询失败必须表达为 `UNKNOWN`，不能继续执行会改变包状态的清理动作。

---

## 8. Domain 边界

### 8.1 FreezeUseCase

- 是所有冻结、解冻、卸载特例、批量和清理动作的业务入口。
- 不拼接任意 shell 字符串。
- 不创建 Toast、Notification 或 Activity。
- 统一返回结构化的成功、失败、未知和跳过结果。
- Recent 单包执行继续走 `freezeApp()`，不能进入全局批量进度。

### 8.2 批量执行

当前 `BatchExec` 的行为先保持稳定，后续逐步将全局 `BatchProgress` 改为操作级状态：

- 每次操作拥有 `OperationId`。
- 进度包含总数、已完成、成功、失败和取消状态。
- 不同入口不能覆盖彼此的进度。
- UI 收集 ViewModel 暴露的 StateFlow，不直接操作全局仓库。

这项迁移不能和第一轮 QuickToggle 数据迁移混在同一个大提交中。

### 8.3 Organize 和 Folder

整理目录已有明确状态机，v0.3 适合把它抽成纯 Kotlin：

```text
None
HomeApp(A)
Folder(F)
AppToFolder(A, F)
FolderMember(F, M)
CreatingFolder(F)
```

`OrganizeReducer` 只处理状态转移和结构操作意图；Repository 负责即时持久化；Compose 负责高亮和按键绘制。

循环滑动抽为纯计算的 `FolderPagePlanner` 或 `SlideLoopUseCase`：

- 输入主屏和文件夹排序。
- 输入是否循环、排除哪些 folder ID。
- 输出稳定的页面序列和边界行为。
- 不持有 `PagerState`，不调用 Compose API。

### 8.4 Recent

Recent 的拆分顺序固定为：

1. `RecentSessionState`：纯会话字段和基线状态。
2. 快照差异：`old - new`、可靠性和空快照保护。
3. 候选策略：已添加、未锁定、自身排除。
4. 队列策略：成功移除、失败保留、重连补执行。
5. 单包冻结结果。
6. 最后缩小 `RecentSwipeController`。

仍留在 service 的内容：

- AccessibilityEvent。
- Handler 和事件去抖。
- AccessibilityNodeInfo 遍历。
- `dumpsys activity recents` 调用。
- 服务连接、销毁和重连。

Recent 首次基线不冻结、校准不冻结和自身包名排除必须先有单元测试，再移动代码。

### 8.5 LockClean

未来新增 `domain/lockclean/LockCleanUseCase`，由它计算：

```text
已添加应用
- 锁定应用
- 例外目录应用
- 已确认处于冻结状态的应用
```

未知状态不能当成未冻结。`LockCleanReceiver` 只负责广播、AlarmManager、Keyguard 和结果通知；耗时工作使用可靠的后台生命周期方案，不在 `onReceive()` 中启动无保护裸线程。

---

## 9. UI 和 Feature 边界

### 9.1 AppShell

新增 `app/AppShell.kt` 和 `app/AppDestination.kt`。

AppShell 是唯一负责页面组合的地方，可以 import feature route，但 feature 之间不能互相 import。

第一阶段不重写所有 UI，只把页面选择和覆盖关系从 HomeScreen 中抽出。旧 Home 逻辑可以暂时作为内部 Content，逐步拆分。

### 9.2 Home

目标拆分：

- `HomeRoute`：收集 ViewModel、处理副作用和导航回调。
- `HomeContent`：页面布局。
- `HomeGrid`：主屏网格。
- `HomeDock`：底部图标栏。
- `HomeDialogs`：确认、批量、权限和结果弹窗。

新组件只接收 `state + callback`。不直接调用 Repository、EngineManager、Service 或 SharedPreferences。

HomeViewModel 不再暴露 Compose `ImageBitmap` 等视觉类型。应用图标由 UI 层根据包名和 `AppIconLoader` 加载，避免 ViewModel 被视觉实现绑死。

### 9.3 Settings

- SettingsRoute 负责状态收集。
- SettingsContent 负责设置项绘制。
- Backup、Recent 校准、锁屏清理和壁纸操作通过 callback 或对应 ViewModel。
- Settings 页面不直接 import Service。
- Settings 页面不直接构造 UseCase。

### 9.4 无状态 Compose

- Composable 只接收不可变 state 和 callback。
- 业务判断、过滤、排序和状态转移放 ViewModel/domain。
- 颜色、间距、动画、霜化和图标视觉放 `ui`。
- 不在 v0.3 架构迁移中同时进行大规模颜值重做。

---

## 10. 面向未来功能的扩展地图

以下是预留边界，不代表 v0.3.0 直接实现。

| 功能 | 建议落点 | 必须提前解决的合同 | v0.3 行动 |
|---|---|---|---|
| 应用分身/多用户 | `core/model/AppTarget`、`domain/appclone`、`data` 用户仓库、`feature/appclone` | user/profile、安全、备份、目标状态 | 定义模型和迁移原则，不实现命令 |
| 休眠模式 | `core/mode`、引擎能力、`domain/control` | `SUSPEND` 状态、引擎能力、查询失败 | 保留能力扩展点，不开放 UI |
| Device Owner | `core/engine/impl/DoEngineImpl.kt` | 能力检测、身份记录、组合操作结果 | 保持空壳，禁止 UI 直接依赖 |
| Root | `core/engine/impl/RootEngineImpl.kt` | 身份一致性、命令白名单、状态恢复 | 保持空壳，禁止自动切换 |
| 禁用/隐藏 | `core/mode`、`domain/control` | desired/observed state、多模式解冻 | 只记录模式设计，不开放 |
| 图片壁纸 | `data` URI、`platform/media`、`ui/BackgroundLayer` | SAF 权限、URI 失效、大图采样 | 预留 URI 语义，不实现选择器 |
| 文件夹高级设置 | `domain/folder`、Settings、`FolderPagePlanner` | folder ID、首尾行为、Pager 越界 | 先抽纯页面规划器 |
| 锁屏例外目录 | `domain/lockclean`、LockCleanPolicy | 例外目录、未知状态、用户范围 | 先收拢清理候选计算 |
| 固定桌面快捷方式 | `domain/shortcut`、`service` 系统入口 | action 白名单、包名校验、OEM 行为 | 不实现固定请求 |
| 应用桥 | `domain/bridge`、受控 exported Activity | Intent、URI 权限、临时解冻策略 | 保持最后优先级 |

未来接口必须遵守：先有一个真实调用者，再创建接口；不能为“以后可能有”创建大量没有行为的抽象。

---

## 11. v0.3 分阶段任务清单

### V03-00：基线和合同冻结

状态：`[x]`

目标：在改代码前把行为和协议写成可验证清单。

涉及：

- `v0.3task.md`
- `AGENTS.md`
- 当前测试目录
- 现有 SP/JSON 和 Binder 实现

工作：

- 建立 SP key、Backup v1、Binder 和文件安全清单。
- 建立 Recent、整理目录和批量执行的行为样例。
- 明确 `v0.2.2` 只读基线。

验收：

- `git diff --check` 通过。
- 行为不变量可逐条对应到测试或真机步骤。
- 没有修改现有业务行为。

建议提交：`docs: 建立 v0.3 模块化施工计划`

### V03-01：组合根和进程启动

状态：`[~]`

目标：所有 Android 入口从同一组合根获得依赖。

新增或修改：

- `app/SnowHideApplication.kt`
- `app/CompositionRoot.kt`
- `app/AppContainer.kt`
- `MainActivity.kt`
- `ShortcutActionActivity.kt`
- `QuickToggleTileService.kt`
- `LockCleanReceiver.kt`
- `LockCleanAccessibilityService.kt`
- `AndroidManifest.xml`

要求：

- 初始化幂等、线程安全、轻量。
- Tile 冷启动能获得引擎和 Grid。
- 不重复构造 UseCase。
- 不重复注册锁屏动态广播。
- 不把应用列表和图标预热塞进 Application。

验收：

- Activity、Shortcut、Tile、Receiver、AccessibilityService 分别冷启动。
- Shizuku 未启动、未授权、重连三种状态不崩溃。
- Debug/Release 编译通过。

建议提交：`refactor: 统一应用组合根与冷启动入口`

### V03-02：受控命令和 Binder 生命周期

状态：`[~]`

目标：不改变 Shizuku UserService 协议，收紧命令和连接边界。

涉及：

- `core/model/PackageName.kt`
- `core/engine/impl/ShizukuEngineImpl.kt`
- `core/engine/impl/ShellCommandService.kt`
- `core/engine/Engine.kt`
- `FreezeUseCase.kt`
- `BatchExec.kt`
- `FreezeExecutor.kt`

要求：

- 业务层不再直接拼接任意 shell 参数。
- 包名格式校验、自身包名排除、必要的 PackageManager 存在性校验分层处理。
- 卸载作为单独的危险操作。
- Binder bind 使用统一连接状态机、超时和取消处理。
- stdout/stderr 并发读取，增加进程超时和输出限制。
- 不修改 descriptor、事务码、UserService 参数和 R8 keep 规则。

验收：

- 合法包名命令保持原行为。
- 非法包名不能进入高权限命令。
- Binder 断开、重连、并发调用不永久挂起。
- Release R8 下冻结、解冻、批量命令可执行。

建议提交：`refactor: 收紧特权命令与引擎连接边界`

### V03-03：QuickToggle 数据所有权

状态：`[~]`

目标：成员和 opened 状态只有一个数据所有者。

新增：

- `data/repo/QuickToggleRepository.kt`
- 必要的 `domain/quicktoggle` 结果模型

迁移：

- `QuickToggleViewModel` 不再直接访问 SP/JSON。
- `QuickToggleUseCase` 不再依赖 SharedPreferences。
- `QuickToggleTileService` 不再解析 JSON。
- 原 key、顺序、成员同步和锁定规则保持不变。

验收：

- 进程重启后成员和 opened 保持。
- 已添加列表移除应用后成员自动剔除。
- 点亮/熄灭、锁定跳过和失败结果符合旧行为。
- Tile 冷启动和主界面同时操作不互相覆盖。

建议提交：`refactor: 集中快速启停数据所有权`

### V03-04：Backup 业务边界

状态：`[~]`

目标：SAF、校验、业务结果和低层 SP/JSON 读写分离。

新增或修改：

- `domain/backup/BackupUseCase.kt`
- `feature/settings/BackupViewModel.kt`
- `data/repo/BackupRepository.kt`
- `SettingsScreen.kt`

要求：

- v1 文件继续可读。
- version、section、字段类型和 key 白名单校验。
- 先完整校验，再写入。
- 不恢复 Recent 队列、冻结缓存、pending alarm 和进行中的进度。
- 使用可确认落盘的写入方式，不能写完后立即杀进程导致半导入。
- SettingsScreen 只处理 Uri、事件和反馈。

验收：

- debug 导出、release 导入并重启后配置保持。
- 坏 JSON、错误 version、未知危险 key 不产生部分导入。
- 旧 v1 备份可导入。

建议提交：`refactor: 建立备份业务边界`

### V03-05：AppShell 和页面边界

状态：`[~]`

目标：停止 feature 互相 import，给后续 UI 优化建立安全壳。

新增或修改：

- `app/AppShell.kt`
- `app/AppDestination.kt`
- `feature/home/HomeRoute.kt`
- `feature/settings/SettingsRoute.kt`
- `feature/about/AboutRoute.kt`
- `HomeScreen.kt`
- `AboutScreen.kt`
- `SwipeDisableSettingsScreen.kt`

要求：

- AppShell 统一页面组合。
- Home 先拆 Route/Content，不同时重写业务。
- Settings Recent 校准改 callback/domain 门面。
- About 不依赖其他 feature ViewModel。
- ViewModel 不暴露 Compose 视觉类型。

验收：

- 主屏、文件夹、设置、关于、应用管理、快速启停导航行为不变。
- Pager、手势、对话框和返回键行为不变。
- 新增 feature 不再 import 其他 feature。

建议提交：`refactor: 建立 AppShell 页面组合边界`

### V03-06：纯状态机和规划器

状态：`[~]`

目标：把最容易测试的业务状态从 Android 生命周期中抽出。

优先顺序：

1. `domain/organize/OrganizeState.kt`
2. `domain/organize/OrganizeReducer.kt`
3. `domain/folder/FolderPagePlanner.kt`
4. `domain/recent/RecentSessionState.kt`
5. Recent 快照差异和过滤 UseCase

要求：

- 不搬 Handler、AccessibilityNodeInfo 或 Compose PagerState。
- 每个状态转移有纯单元测试。
- Recent 基线、校准、退出和自身排除行为保持不变。
- 整理目录即时应用和双高亮规则保持不变。

验收：

- 纯逻辑可以在 JVM unit test 中运行。
- 真机 Recent 与整理目录回归通过。
- Controller 变薄但不改变时序。

建议提交：`refactor: 抽取目录与 Recent 纯状态逻辑`

### V03-07：反馈和平台适配

状态：`[~]`

目标：让 service 不再直接依赖 UI 工具。

涉及：

- `core/feedback`
- `platform/feedback`
- `platform/recent`
- Recent、Tile、Shortcut、LockClean 入口

要求：

- domain 返回结果，不直接发 Toast/通知。
- 前台 Snackbar、后台通知、震动分别由适配器处理。
- ColorOS 后台通知兜底保持。
- Accessibility 读取、SAF、系统包管理等 Android API 不进入纯 domain。

验收：

- 前台、后台、快捷方式、磁贴和无障碍反馈符合旧行为。
- Service 和 domain 不新增 UI import。

建议提交：`refactor: 隔离平台反馈与系统适配`

### V03-08：测试和 v0.3.0 发布门

状态：`[~]`

目标：给架构改动建立最小自动化保护。

必须具备：

- `PackageName` 和受控命令测试。
- QuickToggleRepository/UseCase 测试。
- Backup v1 导入导出和坏数据测试。
- OrganizeReducer 和 FolderPagePlanner 测试。
- RecentSessionState 和首次基线测试。
- FreezeUseCase Fake Engine 测试。
- Grid 旧 JSON 和锁定字段迁移测试。
- Binder 重连和超时至少有可测试的状态层。

发布门：

- Debug 编译通过。
- Release 编译通过。
- Release R8 Shizuku UserService 真机通过。
- 关键真机回归通过。
- 用户确认第一个 v0.3 APK。
- 确认后才提交最终发布 commit、push 和 `v0.3.0` tag。

建议提交：`test: 建立 v0.3 架构回归测试`

---

## 12. 测试和验收矩阵

### 12.1 静态检查

- `git status --short --branch`
- `git diff --check`
- 检查新增 import 是否违反依赖方向。
- 检查 `EngineRegistry.init` 是否只由组合根调用。
- 检查 UseCase 是否只由组合根构造。
- 检查新增代码是否直接读写 SharedPreferences。
- 检查新增代码是否直接调用 Service、Toast、Notification 或 Compose。
- 检查不存在新的任意 `sh -c` 拼接。
- 检查不存在文件删除或非卸载场景卸载。
- 检查 `versionCode` 仍为 `1`。

### 12.2 单元测试

#### QuickToggleRepository

- JSON 正常读取和写入。
- 重复成员去重。
- 空数据和坏数据回退。
- opened 快照保存、读取、清空。
- 旧 key 兼容。

#### QuickToggleUseCase

- 已添加过滤。
- 当前冻结过滤。
- 锁定应用熄灭时跳过。
- 批量失败时结果不伪装为成功。

#### BackupUseCase

- v1 全量合法导入。
- 坏 JSON。
- 错误 version。
- 未知 key。
- 非法包名。
- 导入失败不产生部分写入。
- 运行时队列不被配置备份恢复。

#### Command/Engine

- 合法包名。
- 非法包名和 shell 元字符。
- 自身包名排除。
- Binder 连接、断开、重连、超时和取消。
- Fake Engine 的冻结、解冻、失败、未知结果。

#### Organize/Folder/Recent

- 整理目录六类状态。
- 双高亮焦点规则。
- 到边界左右键不循环。
- 文件夹删除后的成员续补。
- 循环开关和排除目录页面规划。
- Recent 第一份快照不冻结。
- Recent 校准不冻结。
- `old - new` 差异准确。
- 雪藏自身排除。
- 失败队列保留。

### 12.3 真机验收

- Shizuku 未运行。
- Shizuku Binder 重连。
- Shizuku 未授权和已授权。
- MainActivity 冷启动。
- ShortcutActionActivity 冷启动。
- Quick Settings Tile 冷启动。
- 无障碍服务冷启动和重连。
- 锁屏 Receiver 冷启动。
- 单个冻结和解冻。
- 100 个以上应用批量执行，无 ANR。
- QuickToggle 进程重启后状态保持。
- Recent 首次校准不误冻。
- Recent 滑卡只处理已添加、未锁定应用。
- Recent 队列失败保留、服务重连补执行。
- Release R8 下 UserService 正常执行。
- Debug 导出、Release 导入、重启后配置保持。
- 全流程确认不删除目标应用数据。

### 12.4 构建命令

遵守项目已有 Windows Java 构建方式，不在 WSL 直接运行 `./gradlew`：

```text
/mnt/c/Windows/System32/cmd.exe /c "cd /d D:\GitHub\Android\SnowHide && build_debug.bat"
/mnt/c/Windows/System32/cmd.exe /c "cd /d D:\GitHub\Android\SnowHide && build_release.bat"
```

---

## 13. 维护性规则

### 13.1 一个功能一个边界

每次修改必须先写清：

1. 功能真正属于 UI、feature、domain、data、service、platform 还是 core。
2. 谁拥有输入数据。
3. 谁决定业务规则。
4. 谁执行 Android 副作用。
5. 谁向 UI 返回结果。

一个小功能最多附带一个相邻的局部解耦动作，不进行全局重构。

### 13.2 不允许“万能类”

禁止继续扩大以下类型：

- 一个同时负责导航、业务和 UI 的 `HomeScreen`。
- 一个同时负责解析、状态机、队列和执行的 Service Controller。
- 一个包含所有 SP key 的万能 SettingsRepository。
- 一个可以执行任意命令的万能 shell 方法。
- 一个所有操作共享的全局进度变量。
- 一个让所有 feature 都依赖的上帝 ViewModel。

如果一个类开始拥有两个以上不同生命周期或不同变化原因，优先拆边界。

### 13.3 新接口的判断标准

新增接口前必须有：

- 至少一个真实调用者。
- 明确的输入、输出和失败语义。
- 至少一个 Fake 或测试替身的使用场景。
- 不把 Android 类型泄漏到纯业务层。

没有真实调用者的未来功能只记录在本文件的扩展地图中，不创建空壳接口。

### 13.4 错误和未知状态

- 失败不能静默转为成功。
- 查询失败不能静默转为“未冻结”。
- 批量必须能区分成功、失败、跳过、取消和未知。
- 外部命令必须保存可诊断但受限长度的错误信息。
- UI 可以展示简化提示，但不能丢失 domain 的结构化结果。

### 13.5 Android 生命周期

- Service、Receiver、Tile 和 Activity 都可能在没有 MainActivity 的情况下冷启动。
- 任何入口不得假设其他入口已经初始化。
- `onReceive()` 不启动没有生命周期保障的裸线程。
- Binder、协程、Handler 和进程销毁必须有取消路径。
- 所有耗时 Binder、PackageManager 批量查询和 `dumpsys` 都在 IO 线程执行。

---

## 14. 风险记录和处理策略

| 风险 | 影响 | 处理策略 | 阶段 |
|---|---|---|---|
| 组合根初始化遗漏 | Tile/Receiver 冷启动失败 | Application + 幂等 CompositionRoot + 逐入口回归 | V03-01 |
| shell 参数注入 | 高权限执行错误命令 | PackageName 校验、受控命令、卸载单独隔离 | V03-02 |
| Binder 永久等待 | 冻结/解冻卡死 | Mutex、超时、取消、重连状态机 | V03-02 |
| Recent 空快照误冻 | 批量误操作 | 快照可靠性、连续确认、纯状态测试 | V03-06 |
| Backup 部分导入 | 数据损坏或恢复队列 | 完整校验后写入、排除瞬态状态 | V03-04 |
| 旧 SP 不兼容 | 用户布局/锁定丢失 | golden fixture、迁移测试、原 key 保留 | V03-03/V03-04 |
| Home 拆分回归 | 主屏手势和导航异常 | 先拆 Route/Content，不同步重写业务 | V03-05 |
| Recent 一次性重写 | 真机行为回退 | 纯状态先行，每一步独立验证 | V03-06 |
| 多引擎自动切换 | 无法解冻或误操作 | 记录 ControlIdentity，禁止隐式切换 | 未来多引擎 |
| 多用户数据串包 | 冻结错误用户应用 | AppTarget 和 schema migration 先于应用分身 | 未来应用分身 |
| 过早引入抽象 | 代码空壳化 | 真实调用者原则，按阶段创建文件 | 全程 |

---

## 15. 设计文档冲突记录

当前设计文档 `/home/wywy521/opencode_dev/docs/冻结工具设计.md` 与当前实现/AGENTS 存在历史差异。v0.3 施工以当前用户拍板和已验证代码为准，并在需要时单独同步设计文档，不在本次架构代码中偷偷改变事实来源。

已知差异：

1. 设计文档旧版写 Room；当前用户已经拍板 SP+JSON，v0.3 不引入 Room。
2. 设计文档列 haze；当前项目未引入，v0.3 不添加无实际刚需的三方库。
3. 设计文档中的 `PowerEngine` 示例签名已过期，以当前 `Engine.kt` 和 UserService 协议为准。
4. 透明壁纸已经完成，图片背景仍未真正渲染。
5. 快速启停、Recent、备份等功能已经提前完成，路线图文字滞后。
6. AGENTS 中设计文档旧路径与实际路径不同，以实际存在的 `/home/wywy521/opencode_dev/docs/冻结工具设计.md` 为准。
7. 齿轮菜单、锁屏延迟和快捷方式数量等产品描述存在历史版本差异，新增功能前必须先确认当前产品口径。

不允许在架构迁移过程中顺便修改这些产品规格；需要修改时单独记录决策。

---

## 16. 提交和发布策略

### 16.1 文档阶段

发布前曾为：

- `v0.3task.md` 已创建并作为施工记录持续更新。
- v0.3 代码施工已经开始，`versionName` 已为 `0.3.0`，`versionCode` 仍为 `1`。
- 代码可以按小功能编译验证；未经用户明确授权不 push、tag 或发布。

当前：

- 用户已确认第一个 v0.3 APK；最终发布 commit 为 `0ac9e93`，`master` 与 `v0.3.0` 已推送。

### 16.2 代码阶段

开始第一批 v0.3 代码时：

- 修改 `versionName = "0.3.0"`。
- `versionCode` 仍为 `1`。
- Debug 显示 `0.3.0-debug`。
- 编译时间继续使用独立的 `build_timestamp`。
- 代码改动分阶段提交。

建议 commit 顺序：

1. `docs: 建立 v0.3 模块化施工计划`
2. `refactor: 统一应用组合根与冷启动入口`
3. `refactor: 收紧特权命令与引擎连接边界`
4. `refactor: 集中快速启停数据所有权`
5. `refactor: 建立备份业务边界`
6. `refactor: 建立 AppShell 页面组合边界`
7. `refactor: 抽取目录与 Recent 纯状态逻辑`
8. `refactor: 隔离平台反馈与系统适配`
9. `test: 建立 v0.3 架构回归测试`
10. `chore: 发布 v0.3.0`

实际提交可以根据可编译边界调整，但不得把未验证的多个阶段合并成一个无法回滚的大提交。

### 16.3 v0.3.0 发布门

只有同时满足以下条件，才允许创建 `v0.3.0` tag：

- 组合根和所有冷启动入口通过。
- P0 命令安全和 Binder 回归通过。
- QuickToggle 和 Backup 数据兼容测试通过。
- AppShell 迁移后的主屏、设置、Recent 入口真机通过。
- Release R8 下 Shizuku UserService 正常。
- 用户确认生成的第一个 v0.3 APK。
- 用户明确授权提交、推送和打 tag。

---

## 17. 实施记录

以后每完成一个阶段，在这里追加一行，不删除历史记录。

| 日期 | 阶段 | 主要文件 | 行为验证 | commit | 真机结果 | 备注 |
|---|---|---|---|---|---|---|
| 2026-08-22 | V03-00 文档创建 | `v0.3task.md` | `git diff --check` | `429c977` | 未执行 | v0.2.2 基线 |
| 2026-08-24 | V03-01 组合根 | `app/SnowHideApplication.kt`, `app/CompositionRoot.kt`, `app/AppContainer.kt`, Android 入口 | Debug/Release 编译；UseCase 由组合根装配 | `39f698b`, `f50f74d`, `ced9b65`, `b58862b` | 无设备，未冷启动回归 | 待真机验证所有入口 |
| 2026-08-24 | V03-02 受控命令 | `core/model/PackageName.kt`, `core/operation/`, Shizuku/Binder impl | 命令安全、Binder 纯状态单元测试；Release R8 编译 | `8b0276e`, `f72c0f1`；状态补丁未提交 | 无设备，未验证 Shizuku 重连 | 保持 descriptor/事务码不变 |
| 2026-08-24 | V03-03 QuickToggle | `data/repo/QuickToggleRepository.kt`, `domain/QuickToggleUseCase.kt` | Repository Android Test 编译；Fake Engine JVM 测试 | `b4946ff`；失败结果修复未提交 | 无设备，未运行 Android Test | opened 失败时保留快照 |
| 2026-08-24 | V03-04 Backup | `data/repo/BackupRepository.kt`, `domain/backup/`, Settings backup UI | Backup Repository/UseCase Android Test 编译 | `76eefe6`；测试补充未提交 | 无设备，未运行 Android Test | v1 schema 与瞬态字段保持兼容 |
| 2026-08-24 | V03-05 AppShell | `app/AppShell.kt`, `feature/home/HomeRoute.kt`, Settings Recent facade | Debug/Release 编译；feature/service 依赖静态检查 | `bbbb6b0`, `71c61d1`；边界补丁未提交 | 无设备，未导航回归 | Home Content 仍需后续继续拆细 |
| 2026-08-24 | V03-06 纯状态机 | `domain/organize/`, `domain/folder/`, `domain/recent/` | JVM unit tests 通过 | `3550ed0`, `ae2529e`, `c2a0e7f`, `972f8d0` | 无设备，未 Recent/整理目录回归 | 纯逻辑已脱离 Android |
| 2026-08-24 | V03-07 反馈适配 | `core/feedback/`, `platform/feedback/` | Debug/Release 编译；service 无 UI feedback import | `f72c0f1` | 无设备，未前后台反馈回归 | 保留 ColorOS 通知兜底 |
| 2026-08-24 | V03-08 测试/发布门 | `app/src/test/`, `app/src/androidTest/` | `testDebugUnitTest`、`assembleDebugAndroidTest`、`build_debug.bat`、`build_release.bat` 均通过 | `d1b5701`；后续测试/边界补丁未提交 | instrumentation 未运行 | 当时待真机与用户确认，后续结果见 2026-08-25 |
| 2026-08-25 | V03-08 真机回归与发布 | 智能清理、增删应用冻结范围、反馈 Toast、设置提示开关 | `testDebugUnitTest`、`build_release.bat`、`git diff --check`；Release `adb install -r` | `0ac9e93`，`v0.3.0` | 三条真机回归通过；Toast 文案改动待后续单独复核 | `master` 与 tag 已推送 |

每个阶段还应记录：

- 修改前行为。
- 修改后行为。
- 未解决风险。
- 是否需要回滚。
- 用户确认日期。

---

## 18. 最终原则

1. 先保护数据和已验证行为，再谈抽象漂亮。
2. 先建立边界，再添加未来功能。
3. 逻辑状态先纯化，Android 生命周期后适配。
4. 一个数据只有一个所有者，一个业务动作只有一个 UseCase 入口。
5. 一个系统入口只处理系统生命周期，不复制业务规则。
6. 一个引擎只在自己的 impl 隔离区接触第三方 API。
7. 未知状态必须保留为未知，不能为了 UI 方便变成 false。
8. 任何新功能都必须能回答“放在哪一层、谁拥有它、如何测试、失败怎么办”。
9. 不为了完成架构树创建空壳，不为了完成版本号合并未经验证的代码。
10. v0.3 的成功标准不是文件夹变多，而是下一次加功能时不会再扩大耦合。
